# 微信公众平台 API 参考（本技能相关部分）

核准时间：2026-09。接口参数取自微信官方文档原文，权限结论含实测验证。

## 一、核心接口清单

| 步骤 | 方法与路径 | 用途 |
|---|---|---|
| 凭证 | `GET /cgi-bin/token` | 拿 access_token，有效期 7200 秒 |
| 正文图 | `POST /cgi-bin/media/uploadimg` | 上传正文内图片，返回 mmbiz 域名 URL |
| 封面 | `POST /cgi-bin/material/add_material?type=image` | 上传永久素材，返回 media_id |
| 草稿 | `POST /cgi-bin/draft/add` | 新建图文草稿 |
| 发布 | `POST /cgi-bin/freepublish/submit` | 把草稿正式发布出去 |
| 查草稿 | `POST /cgi-bin/draft/batchget` | 列草稿箱 |
| 查详情 | `POST /cgi-bin/draft/get` | 按 media_id 回查草稿内容 |

域名统一 `https://api.weixin.qq.com`。

### 1. access_token

```
GET https://api.weixin.qq.com/cgi-bin/token?grant_type=client_credential&appid=AppID&secret=AppSecret
```

返回 `{"access_token": "...", "expires_in": 7200}`。

- 全局唯一。**在别处刷新会让本地缓存的 token 失效**，报 40001，用 `-f` 强制刷新即可。
- 有调用频率限制，务必用缓存，别每次现取。

### 2. 上传正文图片

```
POST https://api.weixin.qq.com/cgi-bin/media/uploadimg?access_token=TOKEN
Content-Type: multipart/form-data   字段名：media
```

返回 `{"url": "http://mmbiz.qpic.cn/...", "errcode": 0}`。

- 限制：**仅 jpg/png，单张 < 1MB**。
- 不占用素材库 10 万张配额。
- 正文里的**外部图片链接会被微信过滤**，所以正文图必须先过这个接口换链，这是硬要求。

### 3. 上传封面永久素材

```
POST https://api.weixin.qq.com/cgi-bin/material/add_material?access_token=TOKEN&type=image
Content-Type: multipart/form-data   字段名：media
```

返回 `{"media_id": "...", "url": "..."}`。

- 图片上限 10MB，支持 bmp/png/jpeg/jpg/gif。
- 返回的 url 只在**腾讯系域名**内可用，别拿去做外部引用。
- 素材库上限：图文/图片各 10 万，其他类型 1000。

### 4. 新增草稿

```
POST https://api.weixin.qq.com/cgi-bin/draft/add?access_token=TOKEN
Content-Type: application/json
```

请求体：

```json
{
  "articles": [
    {
      "article_type": "news",
      "title": "标题",
      "author": "作者",
      "digest": "摘要",
      "content": "<HTML 正文>",
      "content_source_url": "https://...",
      "thumb_media_id": "封面 media_id",
      "need_open_comment": 0,
      "only_fans_can_comment": 0
    }
  ]
}
```

返回 `{"media_id": "..."}`。

字段约束（超了直接报错，本地先校验更省事）：

| 字段 | 必填 | 上限 | 官方原文口径 |
|---|---|---|---|
| `article_type` | 否 | — | `news`（图文消息）/ `newspic`（图片消息），不填默认 `news` |
| `title` | 是 | **32 字** | 「总长度不超过 32 个字」 |
| `author` | 否 | **16 字** | 「总长度不超过 16 个字」 |
| `digest` | 否 | **120 字** | 「总长度不超过 120 个字」；仅有单图文有摘要，多图文此处为空；留空则自动抓取正文前 **54 字** |
| `content` | 是 | **文档口径：少于 2 万字符、小于 1MB**（实测字符数未强制，1MB 仍有效） | 「会去除 JS」；「涉及图片 url 必须来源『上传图文消息内的图片获取 URL』接口，**外部图片 url 将被过滤**」 |
| `content_source_url` | 否 | **1KB** | 点击「阅读原文」后的 URL |
| `thumb_media_id` | `news` 必填 | — | 「必须是永久 MediaID」 |
| `need_open_comment` | 否 | — | 0 不打开（默认）/ 1 打开 |
| `only_fans_can_comment` | 否 | — | 0 所有人可评论（默认）/ 1 仅粉丝 |
| `image_info.image_list` | `newspic` 是 | **最多 20 张** | 首张图即封面图，元素是永久素材 media_id |
| `cover_info.crop_percent_list[].ratio` | 否 | — | `news` **仅支持 `2.35_1` 与 `1_1`**；`newspic` 支持 `1_1`/`16_9`/`2.35_1` |
| `product_info` | 否 | 商品 ≤50 个（`newspic`） | 未开通带货能力会报 `53404`/`53406` |

> `content` 的官方描述里同时出现「大小不可超过 2kb」和「必须少于 2 万字符，小于 1M」，
> 前者的 2kb 与后者矛盾，按后者的**2 万字符 / 1MB** 执行。
>
> **但 2 万字符这一条实测未强制**（2026-09-17，`draft/add`）：
> 发送 45258 字符 → 返回 `media_id`，回查 `draft/get` 得 45390 字符，尾部与原文一致，
> 即**完整落库、没有截断**（多出的 132 字符是微信自己补的）。所以本技能把
> 「超过 2 万字符」降为 P1 提示，不再阻断。**仍未验证**的是正式发布
> （`freepublish` / 后台点发表）会不会二次校验；真报字数错误再按提示精简即可。
> 1MB 字节数没有放松，仍是 P0。

其他要点：

- 不要传 Unicode 转义（别写 `\u4f5c\u8005`），直接传字符串。
- `crop_percent_list` 的坐标系：图片左上角 `(0,0)`、右下角 `(1,1)`；`x1,y1` 是裁剪区左上角，`x2,y2` 是右下角。传了它微信就不再自动居中裁剪。
- 多图文时只有单图文才有摘要，多图文 `digest` 传空。
- 草稿被群发或发布后，会从草稿箱移除。
- 返回的 `media_id` 不超过 128 字符。

### 5. 发布草稿

```
POST https://api.weixin.qq.com/cgi-bin/freepublish/submit?access_token=TOKEN
Content-Type: application/json        {"media_id": "要发布的草稿 id"}
```

返回 `{"errcode": 0, "errmsg": "ok", "publish_id": "..."}`。

- **`errcode=0` 只代表任务提交成功，不代表发布完成**。最终结果通过开发者服务器配置的事件推送（`PUBLISHJOBFINISH`）返回，`publish_status`：0 成功 / 1 发布中 / 2 原创失败 / 3 常规失败 / 4 平台失败。
- 发布时间可能滞后，提交成功后立刻去后台看可能还没出来。

## 二、权限矩阵与实测结论

微信的接口权限由**账号类型 + 认证状态**共同决定，文档标准口径与实际可用范围存在差异。

| 接口 | 文档口径 | 实测（2026-09-14） |
|---|---|---|
| `/cgi-bin/token` | 所有账号 | ✅ 个人未认证订阅号可用 |
| `/cgi-bin/media/uploadimg` | 所有账号 | ✅ 可用 |
| `/cgi-bin/material/add_material` | 服务号 | ✅ 个人未认证订阅号**可用** |
| `/cgi-bin/draft/add` | 服务号 | ✅ 个人未认证订阅号**可用**，无 48001 |
| `/cgi-bin/freepublish/submit` | 认证账号 | ❌ 个人未认证订阅号报 `48001` |

**关键结论**：

- 个人主体公众号**无法申请微信认证**，所以 API 正式发布对个人号永久不可用，这是账号类型限制而非配置问题。
- 个人号可行路径：**用 API 建草稿 → 后台草稿箱手动点「发表」**。自动化覆盖排版、配图、上传、成稿，只留最后一下人工。
- 非个人主体账号做完微信认证后全链路可用。
- 判断依据始终以公众平台「设置与开发 → 接口权限」页的实际列表为准。

## 三、错误码对照

| 错误码 | 含义 | 处理 |
|---|---|---|
| -1 | 系统繁忙 | 稍后重试 |
| 40001 | AppSecret 错误，或 access_token 失效 | 检查密钥；`token -f` 刷新 |
| 40005 | 文件格式不支持 | 正文图仅 jpg/png |
| 40007 | media_id 无效 | 封面素材可能被删 |
| 40009 | 图片体积/尺寸过大 | 正文图压到 1MB 以下 |
| 40013 | AppID 不合法 | 检查大小写与空格 |
| 40125 | AppSecret 不合法 | 检查大小写与空格 |
| **40164** | **调用 IP 不在白名单** | 见下节 |
| 40243 | AppSecret 已冻结 | 后台解冻 |
| 45009 | 超过每日调用限额 | 减少调用，用 token 缓存 |
| **48001** | **接口未授权** | 账号类型/认证问题，无法绕过 |
| 50004 | 禁止使用 token 接口 | 账号限制 |
| 50007 | 账号已冻结 | — |
| 53404 | 账号被限制带货能力 | 删掉商品参数 |
| 53503 | 草稿未通过发布检查 | 检查草稿内容 |
| 53504 | 请到官网使用该草稿 | 转后台手动发布 |
| 53505 | 请先到官网手动保存成功后再发布 | 转后台手动发布 |

## 四、IP 白名单

微信强制校验**调用方公网出口 IP**，不在白名单一律 40164。

- 白名单入口：**微信开发者平台** `developers.weixin.qq.com/platform` → 我的业务 → 公众号 → 详情页 → **API IP白名单**。
  - 老入口 mp 后台「设置与开发 → 基本配置 → IP白名单」是同一份数据。
  - 需先启用 AppSecret 才能配置白名单。
- 修改后大概率需要**管理员扫码确认**，这是最常被漏掉的一步。
- **只认 IPv4**。`curl ifconfig.me` 可能返回 IPv6，用 `curl -4 ipv4.icanhazip.com` 取 IPv4。
- 白名单有**生效延迟**，不是秒级。用 `scripts/watch_ip.py` 轮询等待，别反复手点。
- 40164 的 `errmsg` **自带被拒 IP**（`invalid ip 36.112.191.145 ...`），脚本会自动抠出来提示。

### 凭证获取路径（2026-09 口径）

AppID / AppSecret 在**微信开发者平台**，不在 mp 后台：

```
企业微信扫码登录 developers.weixin.qq.com/platform
  → 我的业务与服务 → 我的业务 → 公众号 / 服务号
  → 基础信息 → 开发密钥
```

- AppID 明文可见。
- AppSecret 默认隐藏，首次点「启用」→ 明文**只显示一次**，必须当场保存。
- 平台**不存储、不显示**已生成的 AppSecret，忘了只能重置；**重置会让旧密钥立即失效**。
- 启用被拦的两种情况：个人主体未实名（去 mp 后台「设置与开发 → 人员设置 → 管理员信息」补）、非个人主体未做微信认证（需申请微信认证）。

## 五、正文 HTML 的写法约束

公众号编辑器对 HTML 有强过滤，实践要点：

1. **全部用内联样式**。`<style>` 块和 `class` 会被剥离，写了也白写。
2. **不要外部图片**。所有 `<img>` 必须指向 mmbiz 域名（先过 uploadimg），外链会被静默过滤。
3. 不要 JS、不要 iframe、不要表单元素。
4. **所有文字节点用 `<span leaf="">文字</span>` 包裹**。这是粘贴后样式不丢的关键，
   由 `scripts/validate_gzh_html.py` 强制检查。
5. 支持的常用标签：`section`、`p`、`strong`、`em`、`span`、`br`、`img`、`a`、
   `table/thead/tbody/tr/td/th`、`ul/ol/li`、`blockquote`、`code`、`h3`。
   **`<div>` 会被改写**，一律用 `<section>`。
6. 表格要写 `border-collapse: collapse` 并给 `th/td` 内联 padding，否则在手机上会散。
7. 正文区宽度约 677px。正文段落与组件样式**一律从主题组件库取**
   （`references/theme-{标识}.md` + `references/common-components.md`），不要凭记忆手写。
8. **图片自适应、不铺满**：`<img>` 用 `max-width:100%;height:auto;display:block;margin:0 auto`，
   按图片自身尺寸显示并居中；**不用 `width:100%`**（小图会被拉伸变糊）。
   只有表格 / 封面卡 / 流程图这类布局元素才用 `width:100%`。

### 排版与校验的分工

| 关卡 | 脚本 | 查什么 |
|---|---|---|
| 源头关 | `scripts/component_lint.py` | 组件库里的反模式（`white-space:pre`、正文虚线框、平台禁用项） |
| 产物关 | `scripts/validate_gzh_html.py` | 最终 HTML：禁用标签/属性/样式、`<span leaf>` 包裹、半角标点 |

改主题库必跑源头关，改装配逻辑必跑产物关。**ERROR 清零、半角标点 WARNING 也修到 0** 才算完成。

## 六、本地预检口径（`scripts/preflight.py` 的判定依据）

`preflight.py` **只查微信接口侧的硬约束**——就算排版完美，这些问题照样会让接口报错或内容坏掉。
排版问题归上面第五节的双关卡脚本，两类检查刻意分开：改主题时不该动到这里。

**判定依据分四类，可信度不同，看的时候要分清楚**：

| 依据类型 | 含义 | 出错时该怎么办 |
|---|---|---|
| **官方硬约束** | 接口文档原文写明的上限，超了微信直接报错或内容残缺 | 必须改，没有商量余地 |
| **文档口径，实测未强制** | 文档里写了，但实测接口并不校验 | 提示级，照发；真报错再回来处理。**注意这类结论带时间戳，平台改行为就失效** |
| **实测行为** | 平台实际会剥离/过滤的行为，文档没明说 | 改了更稳；确认过无影响可忽略 |
| **经验阈值** | 手机阅读体验的经验值，平台不校验 | 自行判断，不是错 |

### P0 · 阻断

| 编号 | 检查项 | 依据 |
|---|---|---|
| WX000 | `config.json` 不是合法 JSON | 配置完整性 |
| WX001 | 缺少 `appid` / `appsecret` | 官方硬约束 |
| WX002 | `appid` / `appsecret` 还是占位值 | 配置完整性 |
| WX004 | `theme` 标识未注册到 `references/theme-index.md` | 配置完整性（写错等于排版与记录脱节） |
| WX010 | 缺少标题（`article.title` 为空） | 官方硬约束 |
| WX011 | 标题超过 32 字 | 官方硬约束 |
| WX012 | 作者超过 16 字 | 官方硬约束 |
| WX013 | 摘要超过 120 字 | 官方硬约束 |
| WX014 | 原文链接超过 1KB | 官方硬约束 |
| WX020 | 未配置正文文件 | 配置完整性 |
| WX021 | 正文文件不存在 | 配置完整性 |
| WX023 | 正文超过 1MB | 官方硬约束 |
| WX024 | 正文含 `<script>` | 实测行为（会被剥离，且带 JS 有审核风险） |
| WX025 | 正文残留 Markdown 围栏（```） | 完整性检查（原样贴出去就是乱码） |
| WX026 | 正文残留 mermaid 源码块 | 完整性检查（先跑 `render_mermaid.py` 转 PNG） |
| WX027 | 正文含 base64 内嵌图（`data:` URI） | 官方硬约束（会让正文体积失控） |
| WX028 | 正文含外链图（非 mmbiz 域名） | 官方硬约束（外链图被静默过滤） |
| WX029 | 正文图片文件不存在 | 配置完整性 |
| WX030 | 正文图片非 jpg/png | 官方硬约束（`uploadimg` 只收 jpg/png） |
| WX031 | 正文图片超过 1MB | 官方硬约束 |
| WX040 | 未配置封面图 | 官方硬约束（`news` 必填 `thumb_media_id`） |
| WX041 | 封面文件不存在 | 配置完整性 |
| WX042 | 封面格式不支持（非 bmp/png/jpeg/jpg/gif） | 官方硬约束 |
| WX043 | 封面超过 10MB | 官方硬约束（永久图片素材上限） |

### P1 · 警告

| 编号 | 检查项 | 依据 |
|---|---|---|
| WX003 | 没记录主题标识（`config.theme`） | 配置完整性 |
| WX022 | 正文超过 2 万字符（按 HTML 长度，非纯文本）。计数前先剥离 HTML 注释与编辑器注入的记账属性（`data-page-node-id` 等），微信同样会过滤这两类内容 | **文档口径，实测未强制** —— 实测 4.5 万字符被接受且完整落库，见本文第二节 |
| WX102 | 正文过短（不足 200 字符） | 经验阈值 |
| WX103 | 内联事件属性 `on*=` | 实测行为（会被剥离） |
| WX104 | `h1`~`h6` 没写内联 `font-size` | 实测行为（平台对标题标签有默认字号/间距；主题库的 h 组件都显式写了 `font-size`，跟上就不会踩） |
| WX105 | 站内锚点 `<a href="#...">` | 实测行为（公众号不支持页内跳转） |
| WX106 | 图片缺 `src` | 配置完整性 |
| WX107 | 图片没有内联样式 | 经验阈值（改用组件库的 `max-width:100%` 写法） |
| WX108 | 封面比例偏离 2.35:1 | 经验阈值（平台按 2.35:1 展示首图） |
| WX109 | 封面分辨率偏低（宽 <900px），放大显示会糊 | 经验阈值（900px 展示 ×2 屏=1800px） |

### P2 · 建议

| 编号 | 检查项 |
|---|---|
| WX201 / WX202 / WX203 | 摘要留空 / 未配原文链接 / 评论未开启 |
| WX204 | 正文图片较多（>8 张，会拖慢加载） |
| WX205 | 读不出封面像素尺寸（纯标准库解析的边界） |

## 七、明确未覆盖的边界

写文档比写代码更容易越界，这几项**本技能没有验证过**，用的时候以公众平台后台的实际提示为准：

- **群发次数限制**：订阅号、服务号的推送频次是平台运营规则，不是接口文档字段，各账号类型与认证状态下的实际额度以后台提示为准。本技能只负责把内容推进草稿箱，不涉及群发配额计算。
- **审核规则**：正文的合规审核是平台侧的，没有公开的完整词库与判定标准。本技能的自动检查**不覆盖合规判定**，不能替代人工审核。
- **多图文（一次投多篇）**：接口支持，本技能的 CLI 目前只处理单篇。一次草稿里塞多篇需要自己组 `articles` 数组。
- **图片消息（`newspic`）**：接口字段已在第一节记录，但脚本未实现该类型。
- **`crop_percent_list` 的坐标换算**：接口支持，脚本目前不生成该字段，只在体检时提示封面比例。
- **粘贴路径与 API 路径的差异**：`<span leaf="">` 包裹是**手动粘贴**路径的关键（编辑器靠它识别文字节点），
  走 API 建草稿时微信侧更宽松。本技能统一按粘贴口径生成，两条路径都能用。
